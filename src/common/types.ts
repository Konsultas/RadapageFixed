export interface ProfileMeta {
    description:string[],
    imgPath:string,
    title:string,
    name:string
}

export interface PartnerMeta{
    descritpion: string,
    alt: string,
    imagPath:string
}